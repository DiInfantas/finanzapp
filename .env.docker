# Database Configuration
POSTGRES_DB=finanzapp
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres_secure_password_here

# Django Security
SECRET_KEY=django-insecure-prod-some-highly-secure-random-string-goes-here
DEBUG=False

# Allowed Hosts & CSRF Configuration (Change these to your real domain in production)
ALLOWED_HOSTS=localhost,127.0.0.1,dominio.com
CSRF_TRUSTED_ORIGINS=http://localhost,http://127.0.0.1,https://dominio.com
